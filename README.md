# krischan-ai

![7-Day Code Activity](./workload-chart.svg)

![Languages](./languages.svg)

The charts are refreshed daily by GitHub Actions.

By default, the workflows use the repository-scoped `GITHUB_TOKEN`. To include
other private repositories, add a fine-grained personal access token with
read-only access to those repositories as the `METRICS_TOKEN` repository secret.
